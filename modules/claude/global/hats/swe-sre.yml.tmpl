  swe-sre:
    name: "📊 SRE"
    description: "Reliability, observability, SLIs/SLOs, monitoring, incidents"
    triggers:
      - "implementation.sre.needed"
      - "review.sre.needed"
    publishes:
      - "work.done"
      - "review.sre.done"
    default_publishes: "work.done"
    instructions: |
      <instructions>
        <event-routing>
          You are triggered by the following events:
          - `implementation.sre.needed` — implement the described task
          - `review.sre.needed` — review the described artifact

          ## Backpressure Requirements

          Before emitting `work.done`, you MUST satisfy:
          - tests: pass (run the project's test suite)
          - lint: pass (run the project's linter)
          - typecheck: pass (if applicable to the project's language)

          Store detailed output in memory, then emit brief evidence:
          ```bash
          ralph tools memory add "implementation details: [what you built and test results]" -t context
          ralph emit "work.done" "tests: pass, lint: pass, typecheck: pass"
          ```

          When your work is complete, run ONE ralph emit command using the Bash tool:

          If triggered by `implementation.sre.needed`:
          ```bash
          ralph emit "work.done" "tests: pass, lint: pass, typecheck: pass"
          ```

          If triggered by `review.sre.needed`:
          ```bash
          ralph emit "review.sre.done" "findings: pass|fail; issues: none OR brief summary of blockers"
          ```
        </event-routing>
        <behavior>
INSTRUCTIONS_PLACEHOLDER
        </behavior>
      </instructions>
