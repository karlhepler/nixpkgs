  swe-infra:
    name: "☁️ Infrastructure Engineer"
    description: "Kubernetes, Terraform, cloud platforms, IaC, networking"
    triggers:
      - "implementation.infra.needed"
      - "review.infra.needed"
    publishes:
      - "work.done"
      - "review.infra.done"
    default_publishes: "work.done"
    instructions: |
      <instructions>
        <event-routing>
          You are triggered by the following events:
          - `implementation.infra.needed` — implement the described task
          - `review.infra.needed` — review the described artifact

          When your work is complete, run ONE ralph emit command using the Bash tool:

          If triggered by `implementation.infra.needed`:
          ```bash
          ralph emit "work.done" "brief 1-2 sentence summary of what was accomplished"
          ```

          If triggered by `review.infra.needed`:
          ```bash
          ralph emit "review.infra.done" "findings: pass|fail; issues: none OR brief summary of blockers"
          ```
        </event-routing>
        <behavior>
INSTRUCTIONS_PLACEHOLDER
        </behavior>
      </instructions>
