# Ralph Hat Configuration: Monty Burns (Multi-Hat Architecture)
# Generated from Home Manager - do not edit directly
# Source: modules/claude/global/hats/monty-burns.md + modules/claude/global/commands/*.md

event_loop:
  completion_promise: "LOOP_COMPLETE"

cli:
  backend: "claude"

core:
  guardrails:
    - "MANDATORY: Your last action MUST be `ralph emit` via the Bash tool. Describing your work in text does NOT count as completion. The loop WILL FAIL if you do not run ralph emit. Example: `ralph emit \"work.done\" \"summary of what was accomplished\"`"
    - "BLOCKED TOOLS: If any tool call is blocked or denied (including by a PreToolUse hook, even when running with --dangerously-skip-permissions), OR if the same tool call returns an error repeatedly, you MUST immediately run `ralph emit \"work.done\" \"blocker: <what you attempted and what blocked it>\"` via the Bash tool. Explaining the situation in plain text does NOT satisfy this requirement. Do not retry indefinitely — if a tool is structurally blocked, emit failure and stop."
    - "Confine all file operations to the current workspace directory. Do not read or modify files outside the working directory."
    - "For ALL PR comment work use the prc CLI only. NEVER use gh api or gh pr comment. Before replying run: prc list --unresolved --bots-only --max-replies 0. Do not reply to threads that already have replies."
    - "NEVER use the Skill tool. Dispatch all work to specialists exclusively via ralph emit '<event.name>' '<payload>' using the Bash tool."

hats:
HATS_PLACEHOLDER
